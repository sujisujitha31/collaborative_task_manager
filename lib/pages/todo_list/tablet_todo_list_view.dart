import 'package:flutter/material.dart';

class TabletTodoListView extends StatelessWidget {
  const TabletTodoListView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text("we are working in this field"),
    );
  }
}
